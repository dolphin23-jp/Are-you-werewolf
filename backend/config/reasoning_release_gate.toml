[reliability]
max_complete_failure_rate = 0.01
max_discussion_skip_rate = 0.02

[efficiency]
min_mock_logical_call_reduction = 0.50
reference_http_request_reduction = 0.40
reference_total_token_reduction = 0.30

[qualification]
# Was `minimum_live_pairs`, counting seeds that ran both the legacy and the v2
# arm. The legacy arm is no longer run (see scripts/live_ab_reasoning_check.py),
# so that bar became one nothing could clear -- an unsatisfiable criterion is
# not a strict gate, it is a gate that has stopped saying anything. What still
# has to be shown is that v2 itself survived more than one real-provider game.
minimum_live_games = 2
